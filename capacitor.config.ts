import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ph.u1.mcr',
  appName: 'ph-u1-mcr',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
